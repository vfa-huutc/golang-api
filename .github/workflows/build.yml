name: Go Test then Build then Deploy

on:
  push:
    branches:
      - main
      - master
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Go 1.24
        uses: actions/setup-go@v4
        with:
          go-version: '1.24'

      - name: Install gotestsum
        run: |
          go install gotest.tools/gotestsum@latest
          echo "$(go env GOPATH)/bin" >> $GITHUB_PATH

      - name: Run tests with gotestsum and coverage
        run: |
          gotestsum --format=standard-verbose -- -coverprofile=coverage.out ./...
          go tool cover -func=coverage.out
  
  analyze:
    needs: test
    runs-on: ubuntu-latest

    steps:
      - name: Analyze placeholder
        run: echo "🚀 Analyze success (implementation coming soon)"

  build:
    needs: test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Go 1.24
        uses: actions/setup-go@v4
        with:
          go-version: '1.24'

      - name: Build the project
        run: go build ./...

  deploy:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Deploy placeholder
        run: echo "🚀 Deploy success (implementation coming soon)"
